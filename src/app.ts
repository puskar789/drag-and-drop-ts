import { ProjectInput } from "./components/project-input";
import { ProjectList } from "./components/project-list";

// renders the form and lists
new ProjectInput();
new ProjectList("active");
new ProjectList("finished");
